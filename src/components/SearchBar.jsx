import React from 'react';

const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search..."
      className="p-2 w-full bg-gray-600 text-white rounded-lg"
    />
  );
};

export default SearchBar;
